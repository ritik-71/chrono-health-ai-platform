============================================================
ChronoHealth AI Backend — Missing/Mismatched Packages
Generated: 2026-05-11T22:49:00+05:30
============================================================

--- MISSING FROM requirements.txt ---

# Required by core/database.py (SQLite fallback)
pip install aiosqlite

# Required by services/ai_service.py
pip install openai

# Required by core/security.py (JWT decoding)
pip install python-jose[cryptography]

# Required by core/security.py (direct usage, replaces passlib)
pip install bcrypt

--- BROKEN PACKAGE ---

# tensorflow 2.15.0 is incompatible with Python 3.13
# Option A: Remove tensorflow from requirements (LSTM fallback is graceful)
# Option B: Install a compatible version if available:
pip install tensorflow>=2.16.0

--- RECOMMENDED requirements.txt UPDATE ---

# Copy-paste this to replace your current requirements.txt
# to match the actual working environment:

fastapi>=0.110.0
uvicorn>=0.27.1
pydantic>=2.6.3
pydantic-settings>=2.2.1
sqlalchemy>=2.0.27
bcrypt>=4.0.0
python-jose[cryptography]>=3.3.0
pyjwt>=2.8.0
python-multipart>=0.0.9
pandas>=2.2.1
numpy>=1.26.4
scikit-learn>=1.4.0
xgboost>=2.0.0
lightgbm>=4.3.0
joblib>=1.3.2
scipy>=1.12.0
shap>=0.44.0
asyncpg>=0.29.0
aiosqlite>=0.20.0
greenlet>=3.0.0
openai>=1.0.0

# Optional (only if LSTM CII is needed):
# tensorflow>=2.16.0

--- ONE-LINE INSTALL COMMAND ---

pip install aiosqlite openai python-jose[cryptography] bcrypt
